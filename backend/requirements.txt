# =============================================================================
# RouteGuard Backend — Requirements (Hackathon / PostgreSQL-only mode)
# =============================================================================
# Removed: motor (MongoDB), redis, tensorflow (heavy), aiohttp (unused)
# These are no longer needed since we use in-memory stubs.
# =============================================================================

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
websockets==12.0

# Database — PostgreSQL ONLY
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# ML Libraries (scikit-learn + XGBoost — lightweight, no TensorFlow required)
scikit-learn>=1.3.2
xgboost>=2.0.2
numpy>=1.26.2
joblib>=1.3.2
pandas>=2.1.3

# External API client (weather / traffic — graceful fallback if no API key)
httpx==0.25.2

# Background Jobs
apscheduler==3.10.4

# Utilities
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator>=2.0.0
python-dateutil==2.8.2

# Maritime Sea Routing (marnet GeoPackage network)
networkx>=3.2
geopandas>=0.14.0
shapely>=2.0.0
pyogrio>=0.7.0
pyproj>=3.6.0
